<UserControl x:Class="StudentMS.UI.Views.Departments.DepartmentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:StudentMS.UI.Converters"
             Background="{DynamicResource MaterialDesignPaper}"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:InverseBoolConverter x:Key="InverseBool"/>
        <converters:NullOrEmptyToVisibilityConverter x:Key="StringToVisibility"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0"
                Padding="20"
                Margin="0,0,0,16"
                Background="{DynamicResource MaterialDesignCardBackground}"
                CornerRadius="8"
                md:ElevationAssist.Elevation="Dp2">

            <DockPanel LastChildFill="False">

                <StackPanel>
                    <TextBlock Text="Departments"
                               Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>

                    <TextBlock Text="Manage academic departments"
                               Style="{StaticResource MaterialDesignBody2TextBlock}"
                               Margin="0,4,0,0"/>
                </StackPanel>

                <Button DockPanel.Dock="Right"
                        Content="Add Department"
                        Command="{Binding AddDepartmentCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Padding="20,8"
                        md:ButtonAssist.CornerRadius="6"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBool}}"/>
            </DockPanel>
        </Border>

        <!-- MESSAGES -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12">

            <Border Padding="12"
                    CornerRadius="6"
                    Background="#FDECEA"
                    Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibility}}">
                <StackPanel Orientation="Horizontal">
                    <md:PackIcon Kind="AlertCircle" Foreground="#D32F2F" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding ErrorMessage}" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Padding="12"
                    CornerRadius="6"
                    Background="#EDF7ED"
                    Margin="0,8,0,0"
                    Visibility="{Binding SuccessMessage, Converter={StaticResource StringToVisibility}}">
                <StackPanel Orientation="Horizontal">
                    <md:PackIcon Kind="CheckCircle" Foreground="#2E7D32" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding SuccessMessage}" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <ProgressBar IsIndeterminate="True"
                         Height="4"
                         Margin="0,10,0,0"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"/>
        </StackPanel>

        <!-- DATA GRID -->
        <Border Grid.Row="2"
                Padding="8"
                CornerRadius="8"
                Background="{DynamicResource MaterialDesignCardBackground}"
                md:ElevationAssist.Elevation="Dp2">

            <DataGrid ItemsSource="{Binding Departments}"
                      SelectedItem="{Binding SelectedDepartment}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      Style="{StaticResource MaterialDesignDataGrid}"
                      md:DataGridAssist.CellPadding="16 8"
                      md:DataGridAssist.ColumnHeaderPadding="16 10"
                      RowHeight="48">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="ID"
                                        Binding="{Binding DepartmentId}"
                                        Width="80"/>

                    <DataGridTextColumn Header="Department Name"
                                        Binding="{Binding DepartmentName}"
                                        Width="*"/>

                    <!-- ACTIONS -->
                    <DataGridTemplateColumn Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

                                    <Button Style="{StaticResource MaterialDesignToolButton}"
                                            Command="{Binding DataContext.EditDepartmentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}">
                                        <md:PackIcon Kind="Pencil"/>
                                    </Button>

                                    <Button Style="{StaticResource MaterialDesignToolButton}"
                                            Command="{Binding DataContext.DeleteDepartmentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding DepartmentId}">
                                        <md:PackIcon Kind="Delete" Foreground="#D32F2F"/>
                                    </Button>

                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </Border>

    </Grid>
</UserControl>